#This file is generated by updateDaemonJvm
#
# Youer builds require JDK 25 (see java_version in gradle.properties). Pinning the daemon here means
# `./gradlew` runs on 25 no matter which JDK happens to be on PATH. Without it, a JDK 21 daemon
# compiles buildSrc at source level 21 and fails on the unnamed `_` variables NeoForge's build
# plugins use:
#   error: unnamed variables are a preview feature and are disabled by default.
toolchainVersion=25
