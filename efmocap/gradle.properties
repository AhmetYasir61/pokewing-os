# Forge 1.20.1 build properties for EFMocap.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

minecraft_version=1.20.1
forge_version=47.3.0
mapping_channel=official
mapping_version=1.20.1

mod_id=efmocap
mod_name=EFMocap
mod_version=1.3.0
mod_group_id=com.pokewing.efmocap
mod_authors=pokewing
mod_license=MIT

# Epic Fight is accessed via reflection at runtime -- NOT a build dependency.

# Build the EFMocap form type that appears inside BBS's own editor. Needs a
# bbs*.jar in libs/. Off by default: BBS is Fabric-mapped, so parts of its API
# don't resolve against a Mojmap Forge classpath yet.
bbs_form_type=true
